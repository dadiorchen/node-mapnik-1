apiVersion: getambassador.io/v2
kind: Mapping
metadata:
  name: treetracker-tile-server
spec:
  cors:
    origins: '*'
    methods: GET, POST, PATCH, PUT, OPTIONS
    headers:
      - Content-Type
      - Authorization
